<?xml version="1.0" ?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>apigee</groupId>
    <artifactId>parent-pom</artifactId>
    <packaging>pom</packaging>
    <version>1.0</version>
    <pluginRepositories>
        <pluginRepository>
            <id>central</id>
            <name>Maven Plugin Repository</name>
            <url>https://repo1.maven.org/maven2</url>
            <layout>default</layout>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
            <releases>
                <updatePolicy>never</updatePolicy>
            </releases>
        </pluginRepository>
    </pluginRepositories>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <org.slf4j.simpleLogger.defaultLogeLevel>info</org.slf4j.simpleLogger.defaultLogeLevel>
        <project.root.dir>${basedir}</project.root.dir>
        <target.root.dir>${basedir}/target</target.root.dir>
        <api.hosturl>https://api.enterprise.apigee.com</api.hosturl>
        <apigee.apiVersion>v1</apigee.apiVersion>
        <apigee.options>override</apigee.options>
        <apigee.org>${APIGEE_ORG}</apigee.org>
        <apigee.env>${APIGEE_ENV}</apigee.env>
        <apigee.username>${APIGEE_USER}</apigee.username>
        <apigee.password>${APIGEE_PASSWORD}</apigee.password>
        <deployment.suffix></deployment.suffix>
        <feature.name></feature.name>
        <build.identifier>build-identification-info-goes-here</build.identifier>
    </properties>
    <!-- Since Maven 3.0.3, for two plugins bound to the same phase, the order 
         of execution is the same as the order in which you define them. -->
    <build>
        <plugins>
            <plugin>
                <artifactId>maven-clean-plugin</artifactId>
                <version>3.1.0</version>
            </plugin>
            <plugin>
                <artifactId>maven-resources-plugin</artifactId>
                <version>2.6</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <overwrite>true</overwrite>
                            <encoding>UTF-8</encoding>
                            <echo message="basedir parent : ${basedir}" />
                            <outputDirectory>${basedir}/target/apiproxy</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>apiproxy</directory>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>io.apigee.build-tools.enterprise4g</groupId>
                <artifactId>apigee-edge-maven-plugin</artifactId>
                <version>1.2.2</version>
                <executions>
                    <execution>
                        <id>configure-bundle</id>
                        <phase>package</phase>
                        <goals>
                            <goal>configure</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>deploy-bundle</id>
                        <phase>install</phase>
                        <goals>
                            <goal>deploy</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <!-- <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>1.3.2</version>
                <executions>
                    <execution>
                        <id>npm-install</id>
                        <phase>test</phase>
                        <goals>
                            <goal>
                                exec
                            </goal>
                        </goals>
                        <configuration>
                            <executable>npm</executable>
                            <commandLineArgs>install</commandLineArgs>
                        </configuration>
                    </execution>
                    <execution>
                        <id>unit-test</id>
                        <phase>test</phase>
                        <goals>
                            <goal>
                                exec
                            </goal>
                        </goals>
                        <configuration>
                            <executable>npm</executable>
                            <commandLineArgs>run test</commandLineArgs>
                        </configuration>
                    </execution>
                </executions>
            </plugin> -->

            <!-- Run Newman test commandline -->
            <!-- <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>1.3.2</version>
                <executions>
                    <execution>
                        <id>npm-install</id>
                        <phase>test</phase>
                        <goals>
                            <goal>
                                exec
                            </goal>
                        </goals>
                        <configuration>
                            <executable>npm</executable>
                            <commandLineArgs>install</commandLineArgs>
                        </configuration>
                    </execution>
                    <execution>
                        <id>integration-test</id>
                        <phase>install</phase>
                        <goals>
                            <goal>
                                exec
                            </goal>
                        </goals>
                        <configuration>
                            <executable>node_modules/newman/bin/newman.js</executable>
                            <commandLineArgs>run ${project.root.dir}/test/newman/postman-collection.json -e test/newman/environemnt.json</commandLineArgs>
                        </configuration>
                    </execution>
                </executions>
            </plugin> -->

            <!-- Apigee Lint -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>1.3.2</version>
                <executions>
                    <execution>
						<id>npm install (initialize)</id>
						<goals>
							<goal>exec</goal>
						</goals>
						<phase>initialize</phase>
						<configuration>
							<executable>npm</executable>
							<arguments>
								<argument>install</argument>
							</arguments>
						</configuration>
					</execution>
                    <execution>
						<id>npm install (clean)</id>
						<goals>
							<goal>exec</goal>
						</goals>
						<phase>pre-clean</phase>
						<configuration>
							<executable>npm</executable>
							<arguments>
								<argument>install</argument>
							</arguments>
						</configuration>
					</execution>
                    <execution>
						<id>npm config list (validate)</id>
						<goals>
							<goal>exec</goal>
						</goals>
						<phase>validate</phase>
						<configuration>
							<executable>npm</executable>
							<arguments>
								<argument>run</argument>
								<argument>apigeelint</argument>
							</arguments>
						</configuration>
					</execution>
                </executions>
            </plugin>
        </plugins>
    </build>
    <profiles>
        <profile>
            <id>feature</id>
            <properties>
                <deployment.suffix>${feature.name}-</deployment.suffix>
                <proxy.suffix>/${feature.name}-</proxy.suffix>
            </properties>

            <build>
                <plugins>
                    <plugin>
                        <groupId>com.google.code.maven-replacer-plugin</groupId>
                        <artifactId>replacer</artifactId>
                        <version>1.5.2</version>
                        <executions>
                            <execution>
                                <phase>prepare-package</phase>
                                <goals>
                                    <goal>replace</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <basedir>${target.root.dir}</basedir>
                            <includes>
                                <include>apiproxy/proxies/default.xml</include>
                            </includes>
                            <replacements>
                                <replacement>
                                    <token>${proxy.basepath}</token>
                                    <value>${proxy.suffix}{proxy.basepath}</value>
                                </replacement>
                            </replacements>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>deployment</id>
            <properties>
                <deployment.suffix>${feature.name}-</deployment.suffix>
                <proxy.suffix>/${feature.name}-</proxy.suffix>
            </properties>

            <build>
                <plugins>
                    <plugin>
                        <groupId>com.google.code.maven-replacer-plugin</groupId>
                        <artifactId>replacer</artifactId>
                        <version>1.5.2</version>
                        <executions>
                            <execution>
                                <phase>prepare-package</phase>
                                <goals>
                                    <goal>replace</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <basedir>${target.root.dir}</basedir>
                            <includes>
                                <include>apiproxy/*-api.xml</include>
                            </includes>
                            <replacements>
                                <replacement>
                                    <token>#BuildIdentifier#</token>
                                    <value>${build.identifier}</value>
                                </replacement>
                            </replacements>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
    <dependencies>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.8.2</version>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
